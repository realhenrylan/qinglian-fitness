# 更新日志

## 2026-09-23

- 新增网站安全审查报告，记录 PWA、后端 API 和 Netlify 部署配置中的风险与修复建议。
- 修复退出登录或云端令牌失效后账号数据仍残留的问题，并丢弃上一会话迟到的云端响应；保留设备主题设置。
- 修复 Service Worker 缓存认证 API 响应的问题，跳过 `/api` 与携带 `Authorization` 的请求，并升级缓存版本以清理旧缓存。
- 修复云同步的档案、训练和饮食字段进入 HTML 时未转义的问题，避免存储型 XSS。
- 修复并发注册时重复创建同名账号并覆盖密码的问题，改用原子创建并返回冲突响应。
- 修复 PostgreSQL 连接跳过 TLS 证书验证的问题，恢复服务端证书校验。
- 修复 CORS 对所有来源开放的问题，默认只允许同源访问，可按需配置精确的额外来源。
- 为 Bearer 令牌添加 7 天有效期和过期记录清理，并在退出登录时撤销服务端令牌。
- 为 API 响应添加禁止缓存和基础安全头；网站部署增加 CSP 及浏览器安全响应头，并将内联脚本迁出页面。
- 为登录与注册增加 Netlify IP 限流，改用异步密码哈希并限制密码最长 128 个字符。
- 为云同步接口增加字段白名单、类型、数量、长度与范围校验，拒绝数组、异常属性和无效日期。
- 将 Netlify CLI 固定并通过独立锁文件安装，部署与 Netlify 构建改用 `npm ci`；数据库连接缺失时 Netlify Functions 拒绝启动。
